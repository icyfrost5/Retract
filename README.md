# Retract
Heavy WIP, so don't expect the greatest functionalitys right now.


# Usage
Unpack the zip<br>
open cmd inside the zip<br>
use ``` pip install -r requirements.txt ```<br>
Run ```reflex run``` and then go to your default browser and open localhost:3000
